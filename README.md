# Local Agent - Django Project

Проект для управления локальными агентами на Django.

## 🚀 Быстрый старт

### Локально:
```bash
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
pip install -r requirements.txt
python manage.py runserver